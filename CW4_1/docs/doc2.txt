Поток (thread) — это легковесный процесс.
В Java каждый поток имеет свой стек вызовов.
Метод run() содержит код, который будет выполняться в новом потоке.
Вызвать поток можно через start(), а не напрямую run().
Многопоточность позволяет эффективно использовать многоядерные процессоры.